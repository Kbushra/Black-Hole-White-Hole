image_index = differentCol;

var condition = differentCol && global.swap;
var condition2 = !differentCol && !global.swap;
black = condition || condition2;

if differentCol { direction = random(360); speed = 1; }